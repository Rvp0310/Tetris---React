import React from "react";

const StartButton = ({callback}: {callback: string}) => {
    return(
        <div className = 'start'>
            Start
        </div>
    );
}

export default StartButton;